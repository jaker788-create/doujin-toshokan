# Python
.venv/
__pycache__/
*.pyc
doujin.egg-info/

# PyInstaller build artifacts (being retired; doujin.spec committed, outputs are not)
build/doujin/
dist/

# Go / Wails: commit build/ config (icons, manifests, nsis) but NOT the compiled output
build/bin/
node_modules/
frontend/dist/

# Runtime data (kept outside the repo anyway; here as a safety net)
.doujin/
*.db

# Local Claude Code / harness state and AI dev-workflow scaffolding
# (this is an app-only public repo; see the "app-only" scope decision)
.claude/
.claude-plugin/
harness.toml
Plans.md
docs/superpowers/
